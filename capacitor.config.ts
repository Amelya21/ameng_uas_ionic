import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ameng_uassmt3',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
